{
  "private": true,
  "workspaces": [
    "packages/demos/*"
  ],
  "scripts": {
    "clean": "lerna clean && rm -rf node_modules",
    "lint:prettier": "prettier --write --loglevel warn",
    "lint:all:prettier": "yarn lint:prettier \"**/*.{js,css,html,md}\""
  },
  "devDependencies": {
    "lerna": "3.22.1",
    "prettier": "2.0.5"
  }
}
